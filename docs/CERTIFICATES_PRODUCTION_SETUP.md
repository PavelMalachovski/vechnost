# 🎫 Настройка Сертификатов в Production

## 🔍 Проблема

Сертификаты были сгенерированы в **локальной базе данных** (SQLite), но бот работает на **Railway/Render с PostgreSQL**. Поэтому при попытке активации сертификата пользователь получает ошибку "Сертификат не найден".

## ✅ Решение

Нужно добавить сгенерированные сертификаты в production базу данных.

---

## 📋 Сгенерированные Сертификаты

```
1. VECH-7AP3-XSE2455D
2. VECH-ZHYK-VRE75YAG
3. VECH-LHNV-UTDYKQ9T
4. VECH-A9PC-9ABWGGKS
5. VECH-834T-RXRCPJCX
```

QR-коды находятся в папке: `certificates/`

---

## 🚀 Способы Добавления в Production

### Способ 1: Через Python Скрипт (Рекомендуется)

**Шаг 1:** Получите DATABASE_URL из Railway/Render

**Railway:**
```bash
railway variables
```

Или через веб-интерфейс: Settings → Variables → DATABASE_URL

**Render:**
```bash
render env
```

Или через Dashboard → Environment → DATABASE_URL

**Шаг 2:** Запустите скрипт

```bash
# Вариант A: Через переменную окружения
export DATABASE_URL='postgresql://user:pass@host:port/db'
python scripts/add_certificates_to_production.py

# Вариант B: Напрямую как аргумент
python scripts/add_certificates_to_production.py 'postgresql://user:pass@host:port/db'
```

**Ожидаемый вывод:**
```
🎫 Certificate Addition Script
============================================================

📋 Certificates to add:
   1. VECH-7AP3-XSE2455D
   2. VECH-ZHYK-VRE75YAG
   3. VECH-LHNV-UTDYKQ9T
   4. VECH-A9PC-9ABWGGKS
   5. VECH-834T-RXRCPJCX

🔗 Connecting to database...
============================================================
Adding 5 certificates to database...
============================================================

   1. VECH-7AP3-XSE2455D: ✅ Added
   2. VECH-ZHYK-VRE75YAG: ✅ Added
   3. VECH-LHNV-UTDYKQ9T: ✅ Added
   4. VECH-A9PC-9ABWGGKS: ✅ Added
   5. VECH-834T-RXRCPJCX: ✅ Added

============================================================
✅ Successfully processed 5 certificates!
============================================================

✅ Verified: 5/5 certificates in database
```

---

### Способ 2: Через Railway CLI (Быстрый)

**Шаг 1:** Подключитесь к Railway проекту
```bash
railway link
```

**Шаг 2:** Запустите скрипт в Railway окружении
```bash
railway run python scripts/add_certificates_to_production.py
```

Railway автоматически использует правильный DATABASE_URL.

---

### Способ 3: Через SQL Напрямую

Если нет доступа к Python, можно выполнить SQL напрямую.

**Шаг 1:** Подключитесь к БД

**Railway:**
```bash
railway connect postgres
```

**Render:**
Используйте внешний клиент (psql, DBeaver, etc.)

**Шаг 2:** Выполните SQL

```sql
-- Добавить сертификаты
INSERT INTO certificates (code, is_used, created_at) VALUES
  ('VECH-7AP3-XSE2455D', FALSE, CURRENT_TIMESTAMP),
  ('VECH-ZHYK-VRE75YAG', FALSE, CURRENT_TIMESTAMP),
  ('VECH-LHNV-UTDYKQ9T', FALSE, CURRENT_TIMESTAMP),
  ('VECH-A9PC-9ABWGGKS', FALSE, CURRENT_TIMESTAMP),
  ('VECH-834T-RXRCPJCX', FALSE, CURRENT_TIMESTAMP)
ON CONFLICT (code) DO NOTHING;

-- Проверить
SELECT code, is_used, created_at
FROM certificates
WHERE code LIKE 'VECH-%'
ORDER BY created_at DESC
LIMIT 10;
```

---

## ✅ Проверка

После добавления сертификатов проверьте:

### 1. Через бот

Отправьте команду:
```
/activate VECH-ZHYK-VRE75YAG
```

**Ожидаемый результат:**
```
✅ Сертификат успешно активирован!
Добро пожаловать в Vechnost! Теперь у вас есть полный доступ к игре.
```

### 2. Через скрипт проверки

```bash
# Локально
python scripts/check_user_simple.py <telegram_user_id>

# На Railway
railway run python scripts/check_user_simple.py <telegram_user_id>
```

### 3. Через SQL

```sql
SELECT
  c.code,
  c.is_used,
  c.used_by_telegram_user_id,
  c.used_at,
  u.username
FROM certificates c
LEFT JOIN users u ON c.used_by_telegram_user_id = u.telegram_user_id
WHERE c.code LIKE 'VECH-%'
ORDER BY c.created_at DESC;
```

---

## 🎯 Активация Сертификатов Пользователями

### Способ 1: QR-код (Рекомендуется)

1. Распечатайте или отправьте PNG файлы из папки `certificates/`
2. Пользователь сканирует QR-код телефоном
3. Автоматически открывается Telegram бот
4. Сертификат активируется автоматически

### Способ 2: Команда /activate

1. Сообщите пользователю код сертификата
2. Пользователь отправляет боту: `/activate VECH-XXXX-XXXX`
3. Сертификат активируется

### Способ 3: Deep Link

Отправьте ссылку:
```
https://t.me/tvoya_vechnost_bot?start=activate_VECH-7AP3-XSE2455D
```

При переходе по ссылке сертификат активируется автоматически.

---

## 🔒 Безопасность

### ✅ Реализованная защита:

1. **Одноразовое использование** - каждый сертификат можно активировать только один раз
2. **Привязка к пользователю** - сертификат закрепляется за первым активировавшим
3. **Проверка в БД** - все проверки на уровне базы данных
4. **Логирование** - все попытки активации записываются в логи

### ⚠️ Важно:

- **НЕ публикуйте коды сертификатов публично** - кто первый активирует, тот и получит доступ
- **Отслеживайте использование** - проверяйте, кто активировал сертификаты
- **Один сертификат = один пользователь** - повторная активация невозможна

---

## 📊 Мониторинг

### Проверить статус всех сертификатов:

```sql
SELECT
  COUNT(*) FILTER (WHERE is_used = FALSE) as available,
  COUNT(*) FILTER (WHERE is_used = TRUE) as used,
  COUNT(*) as total
FROM certificates;
```

### Посмотреть кто активировал:

```sql
SELECT
  c.code,
  u.username,
  u.first_name,
  c.used_at
FROM certificates c
JOIN users u ON c.used_by_telegram_user_id = u.telegram_user_id
WHERE c.is_used = TRUE
ORDER BY c.used_at DESC;
```

---

## 🐛 Troubleshooting

### Проблема: "Сертификат не найден"

**Причина:** Сертификат не в production БД

**Решение:** Запустите скрипт добавления (см. выше)

### Проблема: "Сертификат уже использован"

**Причина:** Кто-то уже активировал этот сертификат

**Решение:**
1. Проверьте в БД: `SELECT * FROM certificates WHERE code = 'VECH-...'`
2. Если использован по ошибке, можно сбросить:
   ```sql
   UPDATE certificates
   SET is_used = FALSE,
       used_by_telegram_user_id = NULL,
       used_at = NULL
   WHERE code = 'VECH-XXXX-XXXX';
   ```

### Проблема: Бот не отвечает на /activate

**Причина:** Возможно, бот не запущен или DATABASE_URL неверный

**Решение:**
1. Проверьте логи: `railway logs` или Render Dashboard
2. Проверьте переменные окружения
3. Убедитесь, что миграции применены: `railway run alembic upgrade head`

---

## 📝 Создание Новых Сертификатов

Для создания новых сертификатов **сразу в production**:

**Вариант 1: Через Railway**
```bash
railway run python scripts/generate_certificates.py 10
```

**Вариант 2: Локально с production БД**
```bash
export DATABASE_URL='postgresql://...'
python scripts/generate_certificates.py 10
```

Новые QR-коды будут сохранены в `certificates/` и автоматически добавлены в БД.

---

## 🎉 Готово!

После выполнения инструкций:
- ✅ Сертификаты добавлены в production БД
- ✅ Пользователи могут активировать через QR или команду
- ✅ Одноразовое использование гарантировано
- ✅ Полный доступ к боту после активации

**Следующие шаги:**
1. Распространите QR-коды пользователям
2. Следите за активациями в логах
3. При необходимости создавайте новые сертификаты



